# Simplified parameters for a standard generic web deployment
replicaCount: 1

image:
  repository: public.ecr.aws/docker/library/nginx
  tag: alpine

service:
  type: ClusterIP
  port: 80

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 64Mi