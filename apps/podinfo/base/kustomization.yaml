apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Tells Kustomize to download the Helm chart dynamically during compilation
helmCharts:
  - name: podinfo
    repo: https://stefanprodan.github.io/podinfo
    version: 6.7.0
    releaseName: podinfo